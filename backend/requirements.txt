# Lower-bound pins (>=) rather than exact (==), so pip installs recent versions
# that ship prebuilt wheels for new Python releases (e.g. 3.14). Exact-pinning
# old versions forces a source build that can fail on newer interpreters.
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
redis>=5.2.1
pydantic>=2.11
